# Original versus consolidated task responses

Two separate fresh agents answered the same [seven task prompts](tasks.md).
The original arm used only git objects at `abd2767`; the current arm used only
current skills and required references. Neither saw the other arm's output.
Both produced actual artifacts or next-action decisions, not source-review
verdicts. No files, products, runtime missions, or external systems were changed.

| Task | Original response | Consolidated response | Observed preservation |
| --- | --- | --- | --- |
| Rust comment clarity | Read-only; “Pull remote changes” / “Save local edits to disk”; keep distinct names | Read-only; “Pull remote changes” / “Flush local edits to disk”; preserve owners/events | General-language finishing works without TypeScript law or TDD. |
| Finance review guide | Proposed $30k/25% reduction; seat requirement unresolved; quote supports price, not coverage | Same proposed reduction and unresolved seats; explicitly notes actual quote not supplied for inspection | Non-code artifact avoids code axes, approval and publication; intended savings stay separate from observations. |
| Known save sequence | Call tree with unchanged early return and write → invalidate → return | Same sequence, plus concise explanation | Visual explanation does not start an experiment. |
| Immediate standing | PROCEED with owner's accepted slower/simple choice; preference has no blocking standing | Same next action; preserve settlement and material reopening triggers | Merged Steward does not create runtime state or reviewer gates for a dispute. |
| Agent instruction | Mandatory retention pointer before eligibility, both hard guards, all affected tables and changed-table list | Same requirements; unfinished coverage/eligibility remains explicit | Better organization retains reachability, exact constraints, and demand; no formula invented. |
| Typed spec | Direct spec, cancellation question unresolved, vertical RGR plan, no scan or execution | Direct spec, focused grilling on cancellation, full typed flows and vertical plan, no scan or execution | Shared architecture entry keeps sufficient-context path and owner decision boundary. Reading RGR for planning does not execute it. |
| Email publication | Exact Alex/action/date/provenance; generation may precede final recipient checks; revoke invalidates candidate/gates | Same; explicitly binds source/candidate/recipient versions and distinguishes signed renewal from completion | Exact details and scope survive compression; sending requires current support/visibility and revocation handling. |

The current arm loaded only the immediate Steward branch for task 4 and only
visual forms for task 3. Task 6 loaded the RGR reference as planning material;
it explicitly declined implementation and test execution. Task 7 loaded the new
shared derivation contract and all relevant original explanatory references.

These seven single responses are qualitative preservation evidence. They do not
measure invocation frequency, establish statistical improvement, prove every
case, or validate live product behavior. The concrete dispatch consistency
mechanism in task 7 remains an implementation decision in both responses.
