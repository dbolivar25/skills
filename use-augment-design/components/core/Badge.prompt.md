Small status/label pill. Tone carries meaning; soft tint by default, `solid` for emphasis.

```jsx
<Badge tone="success" dot>Success</Badge>
<Badge tone="brand" solid>Brand</Badge>
```

Tones: `neutral`, `brand`, `success`, `warning`, `error`. Props: `solid`, `dot`.
