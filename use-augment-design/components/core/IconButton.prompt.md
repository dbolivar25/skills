Square control for a single icon action (toolbar, close, overflow). Always pass `aria-label`.

```jsx
<IconButton aria-label="Copy" variant="ghost"><CopyIcon size={16} /></IconButton>
```

Variants: `ghost`, `outline`, `solid` (burgundy). Sizes: `sm` 28 / `md` 36 / `lg` 44.
