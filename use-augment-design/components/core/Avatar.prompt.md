Identity primitive. Image with initials fallback; `brand` for the burgundy Augment avatar.

```jsx
<Avatar name="Initials" status="online" />
<Avatar brand name="Augment" size={28} />
```

Props: `src`, `name`, `size`, `status` (online/busy/away), `brand`.
