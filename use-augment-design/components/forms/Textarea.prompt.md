Multi-line text field, styled to match Input.

```jsx
<Textarea label="Field label" rows={5} placeholder="Longer value" />
```

Props: `label`, `hint`, `error`, `rows`. Spreads native textarea attrs.
