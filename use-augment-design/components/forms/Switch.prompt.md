On/off toggle with a sliding knob (gentle overshoot ease). Use for settings, not form submission.

```jsx
<Switch label="Enabled" defaultChecked />
```

Props: `label`, `checked`, `defaultChecked`, `disabled`, `onChange`.
