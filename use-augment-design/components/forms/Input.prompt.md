Single-line text field. Burgundy focus ring; red error state with message below.

```jsx
<Input label="Field label" placeholder="Value" />
<Input label="Field label" error="Value is required" />
```

Props: `label`, `hint`, `error`, `iconLeft`, `prefix`, `size`. Spreads native input attrs.
