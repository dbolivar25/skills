Labeled checkbox, burgundy fill when checked. Controlled or uncontrolled.

```jsx
<Checkbox label="Enabled" defaultChecked />
```

Props: `label`, `checked`, `defaultChecked`, `disabled`, `onChange`.
