Base surface container — white, hairline border, soft shadow, 16px radius. Avoid the colored-left-border trope.

```jsx
<Card padding={24} interactive>
  <h3>Surface title</h3>
  <p>Surface body text.</p>
</Card>
```

Props: `padding`, `interactive` (hover lift), `elevation` 0–3.
