Transient notification card. Stack in a fixed corner; auto-dismiss timing handled upstream.

```jsx
<Toast tone="success" title="Saved">Changes are available.</Toast>
```

Tones: `neutral`, `success`, `error`, `brand`. Props: `title`, `icon`, `onDismiss`.
