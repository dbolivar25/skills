Small dark tooltip on hover/focus. Wraps one trigger child.

```jsx
<Tooltip content="Copy to clipboard"><IconButton aria-label="Copy"><CopyIcon/></IconButton></Tooltip>
```

Props: `content`, `side` (top/bottom/left/right).
