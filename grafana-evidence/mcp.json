{
  "grafana": {
    "command": "pipx",
    "args": [
      "run",
      "mcp-grafana"
    ],
    "env": {
      "GRAFANA_URL": "augmentco.grafana.net",
      "GRAFANA_SERVICE_ACCOUNT_TOKEN": "${GRAFANA_TOKEN}"
    }
  }
}
